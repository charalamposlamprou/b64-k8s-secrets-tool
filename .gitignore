# Virtualenv (recreate with `make setup`)
.venv/

# Runtime state
.app.pid

# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Generated / saved secrets — never commit these
*.yaml
*.yml
.env
sealed-*.yaml

# ...but do track CI workflows
!.github/workflows/*.yml

# macOS
.DS_Store

# Editor / tooling
.claude/
.vscode/
